MONGODB_URI=mongodb+srv://janicah123:<PASSWORD>@cluster0.rcm3enx.mongodb.net/<YOUR_DB_NAME>?retryWrites=true&w=majority
ADMIN_KEY=change_this_to_a_secret_key
PORT=3000

# Notes:
# - Replace `<PASSWORD>` with your database user's password and `<YOUR_DB_NAME>` with the database name (for example: `ecommer`).
# - Never commit a `.env` file containing real credentials to your repository. Use GitHub Secrets or Render environment variables instead.
